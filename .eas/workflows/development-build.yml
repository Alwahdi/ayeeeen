name: Development Build

on:
  push:
    branches: [develop]

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: development

  # build_ios:
  #   type: build
  #   params:
  #     platform: ios
  #     profile: development
